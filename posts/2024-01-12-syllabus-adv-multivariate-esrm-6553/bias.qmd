---
title: "Bias Correction for Varainces"
---

```{r}
Values <- rnorm(10000, mean = 0, sd = 1)
Sigma2 <- var(Values)

S2_get <- function(N) {
  Values_sample <- sample(1:length(Values), N)
  S2 <- var(Sigma)
}
```
